import 'package:flutter/material.dart';

class AttendenceScreen extends StatelessWidget {
  const AttendenceScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Attendence"));
  }
}
